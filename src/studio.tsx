import AdminWorkbench from './v11/AdminWorkbench';import MeasuresWorkbench from './v11/MeasuresWorkbench';import SecurityWorkbench from './v11/SecurityWorkbench';import AIMeasureCatalog from './v11/AIMeasureCatalog';import DataWorkbench from './v11/DataWorkbench';import TransformWorkbench from './v11/TransformWorkbench';import ReportWorkbench from './v11/ReportWorkbench';import SavedReports from './v11/SavedReports';import PublishedViewer from './v11/PublishedViewer';import WorkspaceView from './v11/WorkspaceView';import PackageWorkbench from './v11/PackageWorkbench';import SupabaseAuthGate from './v11/SupabaseAuthGate';import CloudWorkspace from './v11/CloudWorkspace';
import{Component,useEffect,useMemo,useState}from'react';import GridLayout,{Layout}from'react-grid-layout';import{ReactFlow,Background,Controls,MiniMap,Handle,Position,MarkerType,useUpdateNodeInternals}from'@xyflow/react';import{Files,Plus,Database,Filter,Rows3,Link2,Calculator,ShieldCheck,Palette,Type,Tags,Settings2,Play,CheckCircle2,RefreshCcw,ExternalLink,Copy,Trash2,ListTree,PanelBottomClose,PanelBottomOpen,WandSparkles,LayoutGrid,Eye,PanelsTopLeft}from'lucide-react';import Sidebar from'./components/Sidebar';import Topbar from'./components/Topbar';import Chart from'./components/Chart';import{useStudio}from'./store';import{api}from'./api';import type{Visual,VisualType}from'./types';

function Home(){
 const{project,setView,replaceProject}=useStudio();
 const newReport=async()=>{const name=window.prompt('Name your new report','New Analytics Report')?.trim();if(!name)return;const p=await api<any>('/projects/new',{method:'POST',body:JSON.stringify({name})});replaceProject(p);setView('data')};
 return (
  <div className="page lifecycleHomeGlass">
    <div className="heroGlass">
      <span className="eyebrowGlass">VTAB SELF-SERVICE BI</span>
      <h1>Welcome to VTAB Studio.</h1>
      <p>Your workspace for advanced analytics, reporting, and data modeling.</p>
      
      <div className="lifecycleChoicesGlass">
        <button className="choiceCard primaryCard" onClick={newReport}>
          <div className="iconWrap"><Plus size={28}/></div>
          <div className="cardText">
            <b>Start New Project</b>
            <span>Import data and build a report from scratch.</span>
          </div>
        </button>
        <button className="choiceCard" onClick={()=>setView('reports')}>
          <div className="iconWrap"><Files size={28}/></div>
          <div className="cardText">
            <b>Open Saved Reports</b>
            <span>Continue working on your existing projects.</span>
          </div>
        </button>
      </div>
    </div>
    {project && (
      <div className="currentProjectNoteGlass" onClick={()=>setView('report')}>
        <div className="noteHeader"><small>CURRENT ACTIVE PROJECT</small></div>
        <b>{project.report?.name||'Untitled Report'}</b>
        <span>{Object.keys(project.model?.tables||{}).length} tables · {project.transform?.queries?.length||0} queries</span>
      </div>
    )}
  </div>
 );
}

function DataView(){const[meta,setMeta]=useState<any[]>([]);const[selected,setSelected]=useState<any|null>(null);const[connType,setConnType]=useState('demo');useEffect(()=>{api<any[]>('/metadata').then(setMeta)},[]);return <div className="workspace"><div className="pane left"><div className="paneTitle">Sources <button className="icon"><Plus size={15}/></button></div><div className="sourceSearch">Search sources...</div><button className="source active"><Database size={16}/>Demo Sales Database</button><button className="source"><Database size={16}/>SQL Server / Azure SQL</button><button className="source"><Database size={16}/>PostgreSQL</button><div className="hint">Production connectors use the same semantic query contract. Credentials remain backend-only.</div></div><div className="mainPanel"><div className="panelHeader"><div><span className="eyebrow">GET DATA</span><h2>Navigator</h2></div><div className="toolbar"><button><RefreshCcw size={15}/>Refresh metadata</button><button className="primary" onClick={()=>alert('Selected objects are already loaded in the demo semantic model.')}><Play size={15}/>Load selected</button></div></div><div className="navigator"><div className="objectList">{meta.map(t=><button key={t.name} onClick={()=>setSelected(t)} className={selected?.name===t.name?'selected':''}><span>▦</span><div><b>{t.name}</b><small>{t.columns.length} columns</small></div></button>)}</div><div className="objectDetails">{selected?<><div className="objectHero"><div><small>{selected.schema} · TABLE</small><h3>{selected.name}</h3></div><span className="pill">DirectQuery</span></div><table><thead><tr><th>Column</th><th>Type</th><th>Nullable</th><th>Key</th></tr></thead><tbody>{selected.columns.map((c:any)=><tr key={c.name}><td>{c.name}</td><td><span className="typeBadge">{c.type||'TEXT'}</span></td><td>{c.nullable?'Yes':'No'}</td><td>{c.pk?'PK':''}</td></tr>)}</tbody></table></>:<div className="empty">Select a table to inspect its schema.</div>}</div></div></div></div>}

function TransformView(){const{project,update,setView}=useStudio();const[active,setActive]=useState('q-sales');const[rows,setRows]=useState<any[]>([]);const[sql,setSql]=useState('');const q=project?.transform.queries.find((x:any)=>x.id===active);const run=async()=>{if(!q)return;const r=await api<any>('/transform/preview',{method:'POST',body:JSON.stringify({source:q.source,steps:q.steps})});setRows(r.rows);setSql(r.sql)};useEffect(()=>{run()},[active,project]);const addFilter=()=>update(p=>{const x=p.transform.queries.find((z:any)=>z.id===active);x.steps.push({id:crypto.randomUUID(),type:'filter',label:'Keep Quantity >= 2',field:'Quantity',operator:'gte',value:2});return p});return <div className="workspace"><div className="pane left"><div className="paneTitle">Queries</div>{project?.transform.queries.map((x:any)=><button key={x.id} className={'queryItem '+(active===x.id?'active':'')} onClick={()=>setActive(x.id)}><Rows3 size={16}/>{x.name}</button>)}<button className="addSmall"><Plus size={14}/>New query</button></div><div className="mainPanel transform"><div className="panelHeader"><div><span className="eyebrow">TRANSFORMATION ENGINE</span><h2>{q?.name}</h2></div><div className="toolbar"><button onClick={addFilter}><Filter size={15}/>Add Filter</button><button onClick={run} className="primary"><Play size={15}/>Preview</button></div></div><div className="stepRibbon"><button>Select Columns</button><button>Remove</button><button>Rename</button><button>Data Type</button><button>Replace</button><button>Split</button><button>Merge Columns</button><button>Group By</button><button>Join</button><button>Append</button></div><div className="transformBody"><div className="previewGrid"><div className="queryStatus"><span><CheckCircle2 size={14}/>Query folding: full pushdown</span><code>{sql}</code></div><div className="tableWrap"><table><thead><tr>{rows[0]&&Object.keys(rows[0]).map(c=><th key={c}>{c}</th>)}</tr></thead><tbody>{rows.slice(0,40).map((r,i)=><tr key={i}>{Object.keys(r).map(c=><td key={c}>{r[c]}</td>)}</tr>)}</tbody></table></div></div><div className="steps"><h3>Query Settings</h3><label>Name<input value={q?.name||''} readOnly/></label><h3>Applied Steps</h3>{q?.steps.map((s:any,i:number)=><div className="step" key={s.id}><span>{i+1}</span><div><b>{s.label}</b><small>{s.type==='filter'?'Pushdown SQL':'Native source'}</small></div><CheckCircle2 size={15}/></div>)}</div></div></div></div>}

function TableNode({id,data}:any){const updateNodeInternals=useUpdateNodeInternals();return <div className={'modelNode modelNodePro '+(data.fact?'fact':'')}><div className="modelNodeHead custom-drag-handle"><div><b>{data.name}</b><small>{data.fact?'FACT / TRANSACTION':'DIMENSION / LOOKUP'}</small></div><span className="modelFieldCount">{data.columns.length} fields</span></div>{data.hierarchies?.map((h:any)=><div className="modelHierarchy" key={h.id}><ListTree size={13}/><b>{h.name}</b><small>{h.levels.length} levels</small></div>)}<div className="modelFieldsScroll nowheel" onScroll={()=>updateNodeInternals(id)}>{data.columns.map((c:string,i:number)=><div className={'modelField '+(data.relationshipColumns?.has(c)?'relationshipField':'')} key={c}><Handle id={'source-left-'+c} type="source" position={Position.Left} className="fieldHandle fieldHandleLeft"/><Handle id={'target-left-'+c} type="target" position={Position.Left} className="fieldHandle fieldHandleLeft"/><span className="fieldTypeIcon">{data.columnTypes?.[`${data.name}.${c}`]==='date'?'▣':i===0?'◇':/[Aa]mount|[Cc]ost|[Pp]rofit|[Qq]uantity|[Pp]oints|[Dd]ays/.test(c)?'#':'Ab'}</span><span className="fieldName">{c}</span>{data.relationshipColumns?.has(c)&&<span className="relationshipKey">LINK</span>}<Handle id={'source-right-'+c} type="source" position={Position.Right} className="fieldHandle fieldHandleRight"/><Handle id={'target-right-'+c} type="target" position={Position.Right} className="fieldHandle fieldHandleRight"/></div>)}</div></div>}
const nodeTypes={table:TableNode};
function ModelView(){
 const{project,update,setView}=useStudio();const[measureEditorVisible,setMeasureEditorVisible]=useState(false);const[relOpen,setRelOpen]=useState(false);const[aiRelOpen,setAiRelOpen]=useState(false);const[aiRelSuggestions,setAiRelSuggestions]=useState<any[]>([]);const[editingRelationshipId,setEditingRelationshipId]=useState<string|null>(null);const[rel,setRel]=useState<any>({fromTable:'',fromColumn:'',toTable:'',toColumn:'',cardinality:'1:*',filterDirection:'single',active:true});const[nodes,setNodes]=useState<any[]>([]);const[edges,setEdges]=useState<any[]>([]);const[measureName,setMeasureName]=useState('Revenue');const[formula,setFormula]=useState('SUM(Sales.Revenue)');const[validation,setValidation]=useState<any>(null);
 const tables=Object.keys(project?.model.tables||{});
 const factName=useMemo(()=>{const preferred=tables.find(t=>/fact|sales|transaction|orders?/i.test(t));return preferred||tables.sort((a,b)=>Object.keys(project?.model.tables[b]?.columns||{}).length-Object.keys(project?.model.tables[a]?.columns||{}).length)[0]},[project?.model.tables]);
 const arrange=(persist=false)=>{if(!project)return;const rels=project.model.relationships||[];const center=factName||tables[0];const others=tables.filter(t=>t!==center);const pos:any={};pos[center]={x:560,y:280};const radiusX=520,radiusY=330;others.forEach((t,i)=>{const a=(Math.PI*2*i/Math.max(others.length,1))-Math.PI/2;pos[t]={x:560+Math.cos(a)*radiusX,y:300+Math.sin(a)*radiusY}});if(tables.length===2){pos[tables[0]]={x:220,y:280};pos[tables[1]]={x:760,y:280}};setNodes(ns=>ns.map(n=>({...n,position:pos[n.id]||n.position})));if(persist)update(p=>{for(const [name,xy] of Object.entries<any>(pos)){if(p.model.tables[name])Object.assign(p.model.tables[name],xy)}return p})};
 useEffect(()=>{if(!project)return;const relCols:Record<string,Set<string>>={};for(const r of project.model.relationships||[]){(relCols[r.fromTable]??=new Set()).add(r.fromColumn);(relCols[r.toTable]??=new Set()).add(r.toColumn)};const raw=Object.entries<any>(project.model.tables).map(([name,x])=>({id:name,type:'table',position:{x:Number.isFinite(x.x)?x.x:0,y:Number.isFinite(x.y)?x.y:0},data:{name,columns:Object.keys(x.columns),fact:name===factName,columnTypes:project.model.columnTypes||{},hierarchies:(project.model.hierarchies||[]).filter((h:any)=>h.table===name),relationshipColumns:relCols[name]||new Set()}}));const unique=new Set(raw.map(n=>`${Math.round(n.position.x/40)}:${Math.round(n.position.y/40)}`));if(raw.length>1&&unique.size<raw.length){const pos:any={};raw.forEach((n,i)=>{pos[n.id]={x:180+(i%3)*480,y:170+Math.floor(i/3)*390}});raw.forEach(n=>n.position=pos[n.id])}setNodes(raw);const positions=Object.fromEntries(raw.map(n=>[n.id,n.position]));setEdges((project.model.relationships||[]).map((r:any)=>({id:r.id,source:r.fromTable,target:r.toTable,sourceHandle:'source-'+(((positions[r.fromTable]?.x??0)<=(positions[r.toTable]?.x??0))?'right':'left')+'-'+r.fromColumn,targetHandle:'target-'+(((positions[r.fromTable]?.x??0)<=(positions[r.toTable]?.x??0))?'left':'right')+'-'+r.toColumn,label:`${r.cardinality}  ${r.filterDirection==='both'?'Both':'Single'}`,animated:false,type:'straight',markerEnd:{type:MarkerType.ArrowClosed,color:r.cardinality==='*:*'?'#16a34a':'#2563eb'},style:{stroke:r.active===false?'#94a3b8':r.cardinality==='*:*'?'#16a34a':'#2563eb',strokeWidth:3,strokeDasharray:r.active===false?'7 6':undefined},labelStyle:{fill:'#0f172a',fontWeight:800,fontSize:12},labelBgStyle:{fill:'#ffffff',fillOpacity:.96,stroke:'#cbd5e1'},labelBgPadding:[7,4],labelBgBorderRadius:7})))},[project?.model.tables,project?.model.relationships,project?.model.hierarchies,factName]);
 const validate=async()=>setValidation(await api('/measures/validate',{method:'POST',body:JSON.stringify({name:measureName,expression:formula})}));
 const saveMeasure=()=>{if(!validation?.valid)return;update(p=>{p.model.measures[measureName]=formula;return p})};
 const openRel=()=>{const ft=factName||tables[0]||'',tt=tables.find(t=>t!==ft)||tables[0]||'';const common=Object.keys(project.model.tables[ft]?.columns||{}).find(c=>project.model.tables[tt]?.columns?.[c]!==undefined);setEditingRelationshipId(null);setRel({fromTable:ft,fromColumn:common||Object.keys(project.model.tables[ft]?.columns||{})[0]||'',toTable:tt,toColumn:common||Object.keys(project.model.tables[tt]?.columns||{})[0]||'',cardinality:'*:1',filterDirection:'single',active:true});setRelOpen(true)};
 const editRelationship=(id:string)=>{const r=(project.model.relationships||[]).find((x:any)=>x.id===id);if(!r)return;setEditingRelationshipId(id);setRel({fromTable:r.fromTable,fromColumn:r.fromColumn,toTable:r.toTable,toColumn:r.toColumn,cardinality:r.cardinality||'1:*',filterDirection:r.filterDirection||'single',active:r.active!==false});setRelOpen(true)};
 const saveRel=()=>{if(!rel.fromTable||!rel.fromColumn||!rel.toTable||!rel.toColumn)return;update(p=>{p.model.relationships=p.model.relationships||[];const duplicate=p.model.relationships.find((x:any)=>x.id!==editingRelationshipId&&x.fromTable===rel.fromTable&&x.fromColumn===rel.fromColumn&&x.toTable===rel.toTable&&x.toColumn===rel.toColumn);if(duplicate){alert('An identical relationship already exists.');return p}if(editingRelationshipId){const i=p.model.relationships.findIndex((x:any)=>x.id===editingRelationshipId);if(i>=0)p.model.relationships[i]={id:editingRelationshipId,...rel}}else p.model.relationships.push({id:crypto.randomUUID(),...rel});return p});setRelOpen(false);setEditingRelationshipId(null)};
 const deleteRelationship=()=>{if(!editingRelationshipId)return;if(!confirm('Delete this relationship?'))return;update(p=>{p.model.relationships=(p.model.relationships||[]).filter((x:any)=>x.id!==editingRelationshipId);return p});setRelOpen(false);setEditingRelationshipId(null)};
  const autoDetect=()=>{if(!project)return;const suggestions:any[]=[];const normalize=(s:string)=>s.toLowerCase().replace(/[^a-z0-9]/g,'');for(let i=0;i<tables.length;i++)for(let j=i+1;j<tables.length;j++){const a=tables[i],b=tables[j],ac=Object.keys(project.model.tables[a].columns),bc=Object.keys(project.model.tables[b].columns);let best:any=null;for(const ca of ac)for(const cb of bc){const na=normalize(ca),nb=normalize(cb);let score=0;if(na===nb)score+=70;if(/(id|key|code)$/.test(na)&&/(id|key|code)$/.test(nb))score+=15;const aStem=na.replace(/(id|key|code)$/,'');const bStem=nb.replace(/(id|key|code)$/,'');if(aStem&&aStem===bStem)score+=20;if(aStem&&(normalize(a).includes(aStem)||normalize(b).includes(aStem)))score+=5;if(score>(best?.score||0))best={ca,cb,score}}if(best&&best.score>=70){const aFact=a===factName,bFact=b===factName;const from=aFact?a:bFact?b:a,to=aFact?b:bFact?a:b;const fromCol=from===a?best.ca:best.cb,toCol=to===a?best.ca:best.cb;const confidence=Math.min(99,best.score);suggestions.push({id:crypto.randomUUID(),selected:true,fromTable:from,fromColumn:fromCol,toTable:to,toColumn:toCol,cardinality:'*:1',filterDirection:'single',active:true,confidence,reason:best.score>=100?'Matching business key name and ID/key pattern':'Matching key column names detected across model tables'})}}setAiRelSuggestions(suggestions);setAiRelOpen(true)};
  const applyAiRelationships=()=>{const chosen=aiRelSuggestions.filter((s:any)=>s.selected);update(p=>{p.model.relationships=p.model.relationships||[];for(const s of chosen){if(!p.model.relationships.some((r:any)=>r.fromTable===s.fromTable&&r.fromColumn===s.fromColumn&&r.toTable===s.toTable&&r.toColumn===s.toColumn)){const {selected,confidence,reason,...rel}=s;p.model.relationships.push(rel)}}return p});setAiRelOpen(false);setTimeout(()=>arrange(true),80)};
  const onDragStop=(_:any,node:any)=>update(p=>{if(p.model.tables[node.id]){p.model.tables[node.id].x=node.position.x;p.model.tables[node.id].y=node.position.y}return p});
  const onConnect=(params:any)=>{const fromTable=params.source,toTable=params.target;const cleanHandle=(h:any)=>String(h||'').replace(/^(source|target)-(left|right)-/,'').replace(/^(left|right)-/,'');const fromColumn=cleanHandle(params.sourceHandle),toColumn=cleanHandle(params.targetHandle);if(!fromTable||!fromColumn||!toTable||!toColumn||fromTable===toTable)return;update(p=>{p.model.relationships=p.model.relationships||[];if(p.model.relationships.find((x:any)=>(x.fromTable===fromTable&&x.fromColumn===fromColumn&&x.toTable===toTable&&x.toColumn===toColumn)||(x.fromTable===toTable&&x.fromColumn===toColumn&&x.toTable===fromTable&&x.toColumn===fromColumn)))return p;p.model.relationships.push({id:crypto.randomUUID(),fromTable,fromColumn,toTable,toColumn,cardinality:'1:*',filterDirection:'single',active:true});return p})};

  return <div className="modelWorkspace professionalModelWorkspace modelDesignerV27"><div className="modelTop"><div><span className="eyebrow">SEMANTIC ENGINE</span><h2>{project?.name||'Semantic Model'}</h2><p><b>{tables.length}</b> model tables · <b>{(project?.model.relationships||[]).length}</b> relationships. Relationship lines connect the exact key columns.</p></div><div className="toolbar modelToolbar"><button onClick={()=>arrange(true)}><LayoutGrid size={15}/>Auto Arrange</button><button onClick={autoDetect}><WandSparkles size={15}/>AI Relationships</button><button onClick={()=>{const table=window.prompt('Hierarchy table',tables[0]||'');if(!table||!project.model.tables[table])return;const name=window.prompt('Hierarchy name','New Hierarchy');if(!name)return;const available=Object.keys(project.model.tables[table].columns);const levelsText=window.prompt('Levels in drill order, comma separated\nAvailable: '+available.join(', '),available.slice(0,3).join(', '));if(!levelsText)return;const levels=levelsText.split(',').map((x:string)=>x.trim()).filter((x:string)=>available.includes(x)).map((x:string)=>({name:x,field:table+'.'+x}));if(!levels.length)return;update(p=>{p.model.hierarchies=p.model.hierarchies||[];p.model.hierarchies.push({id:crypto.randomUUID(),name,table,auto:false,levels});return p})}}><ListTree size={15}/>Hierarchy</button><button className="primary" onClick={openRel}><Link2 size={15}/>New Relationship</button><button className="aiCatalogModelBtn" onClick={()=>setView('ai-measures')}><WandSparkles size={15}/>AI Measure Catalog</button>{(project.model.relationships||[]).length>0&&<button onClick={()=>editRelationship(project.model.relationships[0].id)}><Settings2 size={15}/>Edit Relationship</button>}<button onClick={()=>setMeasureEditorVisible(v=>!v)}>{measureEditorVisible?<PanelBottomClose size={15}/>:<PanelBottomOpen size={15}/>}{measureEditorVisible?'Hide Measures':'Quick Measure'}</button></div></div><div className="modelLegend"><span><i className="legendLine blue"/>1:* / *:1 relationship</span><span><i className="legendLine green"/>Many-to-many</span><span><i className="legendLine inactive"/>Inactive</span><span><b>LINK</b> = relationship field</span><span><b>TIP</b> Click any relationship line to edit it</span></div><div className="modelCanvas"><ReactFlow nodes={nodes} edges={edges} nodeTypes={nodeTypes} fitView fitViewOptions={{padding:.22}} onNodeDragStop={onDragStop} onEdgeClick={(_,edge)=>editRelationship(edge.id)} onConnect={onConnect} minZoom={.25} maxZoom={1.5}><Background color="#dce5ef" gap={24}/><Controls/><MiniMap pannable zoomable nodeColor="#ffffff" maskColor="rgba(15,23,42,.06)"/></ReactFlow>{!tables.length&&<div className="modelEmptyOverlay"><b>No processed tables in the Model yet</b><span>Go to Transform and use Close & Apply. The processed query will be registered here automatically.</span></div>}{tables.length>1&&!project.model.relationships?.length&&<div className="modelRelationshipHint"><Link2 size={20}/><div><b>No relationships yet</b><span>Use <strong>AI Relationships</strong> to review suggested links, or create one manually.</span></div></div>}</div>{measureEditorVisible&&<div className="measureEditor"><div className="measureHeader"><Calculator size={18}/><b>Quick Measure Editor</b><span>Advanced DAX + AI are available in Measures</span></div><div className="measureFields"><label>Name<input value={measureName} onChange={e=>setMeasureName(e.target.value)}/></label><label className="formula">Formula<textarea value={formula} onChange={e=>setFormula(e.target.value)} spellCheck={false}/></label><button onClick={validate}>Validate</button><button className="primary" onClick={saveMeasure}>Save Measure</button></div>{validation&&<div className={'validation '+(validation.valid?'ok':'bad')}>{validation.valid?<><CheckCircle2 size={15}/>Valid · <code>{validation.compiled}</code></>:validation.error}</div>}</div>}{aiRelOpen&&<div className="modalShade" onMouseDown={()=>setAiRelOpen(false)}><div className="modalCard wide aiRelationshipDialog" onMouseDown={e=>e.stopPropagation()}><div className="modalHead"><div><span className="eyebrow">AI MODEL ASSISTANT</span><h3>Relationship Recommendations</h3></div><button onClick={()=>setAiRelOpen(false)}>×</button></div><p>AI reviewed table names and key-like columns in the current semantic model. Select the relationships you want to create, or edit any recommendation first.</p><div className="aiRelationshipList">{!aiRelSuggestions.length&&<div className="paneEmpty"><Link2/><b>No confident relationship suggestions</b><span>Create a relationship manually, or rename key columns more consistently.</span></div>}{aiRelSuggestions.map((s:any,i:number)=><div className="aiRelationshipRow" key={s.id}><input type="checkbox" checked={s.selected} onChange={e=>setAiRelSuggestions(xs=>xs.map((x:any,j:number)=>j===i?{...x,selected:e.target.checked}:x))}/><div className="aiRelMain"><b>{s.fromTable}[{s.fromColumn}] <span>{s.cardinality}</span> {s.toTable}[{s.toColumn}]</b><small>{s.reason} · {s.confidence}% confidence</small></div><select value={s.cardinality} onChange={e=>setAiRelSuggestions(xs=>xs.map((x:any,j:number)=>j===i?{...x,cardinality:e.target.value}:x))}><option value="1:*">1:*</option><option value="*:1">*:1</option><option value="*:*">*:*</option><option value="1:1">1:1</option></select><select value={s.filterDirection} onChange={e=>setAiRelSuggestions(xs=>xs.map((x:any,j:number)=>j===i?{...x,filterDirection:e.target.value}:x))}><option value="single">Single</option><option value="both">Both</option></select><button onClick={()=>{setAiRelOpen(false);setEditingRelationshipId(null);setRel({fromTable:s.fromTable,fromColumn:s.fromColumn,toTable:s.toTable,toColumn:s.toColumn,cardinality:s.cardinality,filterDirection:s.filterDirection,active:s.active});setRelOpen(true)}}>Edit</button></div>)}</div><div className="dialogActions"><span className="dialogSpacer"/><button onClick={()=>setAiRelOpen(false)}>Cancel</button><button className="primary" disabled={!aiRelSuggestions.some((s:any)=>s.selected)} onClick={applyAiRelationships}>Create Selected Relationships</button></div></div></div>}{relOpen&&<div className="modalShade" onMouseDown={()=>setRelOpen(false)}><div className="modalCard relationshipDialog relationshipDialogPro" onMouseDown={e=>e.stopPropagation()}><div className="modalHead"><div><span className="eyebrow">MODEL RELATIONSHIP</span><h3>{editingRelationshipId?'Edit Relationship':'Create Relationship'}</h3></div><button onClick={()=>setRelOpen(false)}>×</button></div><p>Choose the exact key fields. The link will be drawn directly between those fields in the model canvas.</p><div className="relationshipGrid"><label>From table<select value={rel.fromTable} onChange={e=>{const v=e.target.value;setRel((r:any)=>({...r,fromTable:v,fromColumn:Object.keys(project.model.tables[v]?.columns||{})[0]||''}))}}>{tables.map(t=><option key={t}>{t}</option>)}</select></label><label>From column<select value={rel.fromColumn} onChange={e=>setRel((r:any)=>({...r,fromColumn:e.target.value}))}>{Object.keys(project.model.tables[rel.fromTable]?.columns||{}).map(c=><option key={c}>{c}</option>)}</select></label><label>To table<select value={rel.toTable} onChange={e=>{const v=e.target.value;setRel((r:any)=>({...r,toTable:v,toColumn:Object.keys(project.model.tables[v]?.columns||{})[0]||''}))}}>{tables.map(t=><option key={t}>{t}</option>)}</select></label><label>To column<select value={rel.toColumn} onChange={e=>setRel((r:any)=>({...r,toColumn:e.target.value}))}>{Object.keys(project.model.tables[rel.toTable]?.columns||{}).map(c=><option key={c}>{c}</option>)}</select></label><label>Cardinality<select value={rel.cardinality} onChange={e=>setRel((r:any)=>({...r,cardinality:e.target.value}))}><option value="1:*">One to Many (1:*)</option><option value="*:1">Many to One (*:1)</option><option value="*:*">Many to Many (*:*)</option><option value="1:1">One to One (1:1)</option></select></label><label>Cross-filter direction<select value={rel.filterDirection} onChange={e=>setRel((r:any)=>({...r,filterDirection:e.target.value}))}><option value="single">Single direction</option><option value="both">Both directions</option></select></label><label className="relationshipActive"><span>Status</span><span className="relationshipCheckbox"><input type="checkbox" checked={rel.active!==false} onChange={e=>setRel((r:any)=>({...r,active:e.target.checked}))}/> Active relationship</span></label></div><div className="relationshipPreview"><span>{rel.fromTable}[{rel.fromColumn}]</span><b>{rel.cardinality}</b><span>{rel.toTable}[{rel.toColumn}]</span></div><div className="dialogActions">{editingRelationshipId&&<button className="dangerRelationshipBtn" onClick={deleteRelationship}><Trash2 size={14}/>Delete Relationship</button>}<span className="dialogSpacer"/><button onClick={()=>{setRelOpen(false);setEditingRelationshipId(null)}}>Cancel</button><button className="primary" onClick={saveRel}>{editingRelationshipId?'Save Changes':'Create & Show Link'}</button></div></div></div>}</div>
}
function SecurityView(){const{project,update}=useStudio();if(!project)return null;const active=project.security.activeRoleId;return <div className="page"><div className="panelHeader"><div><span className="eyebrow">SECURITY ENGINE</span><h2>Row-Level Security</h2><p>Security predicates are applied by the semantic query planner before database execution.</p></div></div><div className="securityGrid"><div className="securityCard"><ShieldCheck size={28}/><h3>View as Role</h3><p>Simulate a consumer security context across every report query.</p><select value={active||''} onChange={e=>update(p=>{p.security.activeRoleId=e.target.value||null;return p})}><option value="">No simulation</option>{project.security.roles.map((r:any)=><option value={r.id} key={r.id}>{r.name}</option>)}</select>{active&&<div className="simulation">⚠ Simulation is active</div>}</div>{project.security.roles.map((r:any)=><div className="securityCard" key={r.id}><span className="pill">ROLE</span><h3>{r.name}</h3>{r.rules.map((x:any,i:number)=><div className="rule" key={i}><b>{x.table}.{x.column}</b><span>=</span><code>{x.value}</code></div>)}<button>Edit role</button></div>)}<button className="securityCard addRole"><Plus size={26}/><b>Create security role</b></button></div></div>}
import { isSupabaseConfigured, supabase } from './supabase';
const workspacePortalUrl = (ask = false) => {
  let stored = localStorage.getItem('vtab_web_workspace_url') || '';
  if (stored.includes('localhost:4173')) {
    localStorage.removeItem('vtab_web_workspace_url');
    stored = '';
  }
  const configured = (import.meta.env.VITE_WEB_URL || stored || '').trim().replace(/\/+$/, '');
  if (configured) return configured;
  if (!ask) return '';
  const value = (window.prompt('Web Workspace URL', 'https://reporting-tool-2.vercel.app') || '').trim().replace(/\/+$/, '');
  if (value) localStorage.setItem('vtab_web_workspace_url', value);
  return value;
};
const openWorkspaceReport=(id:string,share=true,target?:Window|null)=>{const base=workspacePortalUrl(true);if(!base){target?.close();return}const url=`${base}/?workspace=1&report=${encodeURIComponent(id)}${share?'&share=1':''}`;if(target)target.location.href=url;else window.open(url,'_blank')};
const cloudSession=async()=>{if(!supabase)throw new Error('Supabase client is not configured.');const{data,error}=await supabase.auth.getSession();if(error)throw error;if(!data.session?.user?.id)throw new Error('Please sign in before publishing to the web workspace.');return data.session};
const cloudPublishProject=async(project:any)=>{await cloudSession();const snapshot=structuredClone(project);const reportId=snapshot.report?.id||crypto.randomUUID();snapshot.report={...(snapshot.report||{}),id:reportId};const name=(snapshot.report?.name||snapshot.name||'Untitled Report').trim();const{data,error}=await supabase!.rpc('publish_report_for_user',{p_report_id:reportId,p_name:name,p_project_json:JSON.stringify(snapshot)});if(error){const m=error.message||String(error);if(/publish_report_for_user|schema cache|function/i.test(m))throw new Error('Supabase publish setup is missing. Run apps/api/supabase_rpc.sql in the Supabase SQL Editor, then retry Publish.');throw error}if(data?.error)throw new Error(data.error);return{ok:true,id:data?.id||reportId,name:data?.name||name,publishedAt:data?.published_at}};
const cloudListPublished=async()=>{const session=await cloudSession();const{data:grants,error:gErr}=await supabase!.from('report_access_grants').select('report_id,role').eq('user_id',session.user.id);if(gErr)throw gErr;if(!grants?.length)return[];const ids=grants.map((g:any)=>g.report_id);const roleMap=Object.fromEntries(grants.map((g:any)=>[g.report_id,g.role]));const{data:rows,error:rErr}=await supabase!.from('published_reports').select('id,name,published_at,project_json').in('id',ids).order('published_at',{ascending:false});if(rErr)throw rErr;return(rows||[]).map((r:any)=>{let p:any={};try{p=JSON.parse(r.project_json||'{}')}catch{}const pages=p.report?.pages||[];return{id:r.id,name:r.name,published_at:r.published_at,pages:pages.length||1,visuals:pages.reduce((n:number,x:any)=>n+(x.visuals?.length||0),0),role:roleMap[r.id]||'Viewer'}})};
function PublishView(){
 const{project,save}=useStudio();
 const[published,setPublished]=useState<any>(null);const[publishedList,setPublishedList]=useState<any[]>([]);const[busy,setBusy]=useState(false);const[err,setErr]=useState('');
 const cloud=isSupabaseConfigured;
 const loadPublished=async()=>{try{const items=cloud?await cloudListPublished():await api<any[]>('/published');setPublishedList(items||[])}catch(e:any){setPublishedList([]);setErr(e?.message||String(e))}};
 useEffect(()=>{loadPublished()},[]);
 const go=async()=>{if(busy||!project)return;const popup=cloud?window.open('about:blank','_blank'):null;setBusy(true);setErr('');setPublished(null);try{if(cloud){try{await save()}catch{}const result=await cloudPublishProject(project);setPublished(result);await loadPublished();openWorkspaceReport(result.id,true,popup)}else{await save();const result=await api<any>('/publish',{method:'POST'});setPublished(result);await loadPublished()}}catch(e:any){popup?.close();setErr(e?.message||String(e))}finally{setBusy(false)}};
 const portalUrl=workspacePortalUrl(false)||'Web Workspace';
 return <div className="page publish publishV27"><div className="publishHero"><div><span className="eyebrow">PUBLISHING ENGINE</span><h1>Publish governed analytics<br/>to VTAB Workspace.</h1><p>Publishing creates a separate consumer-ready snapshot. Users view the report without the authoring panes, model editor, transformations or measure editor.</p></div><button className="workspaceShortcut" onClick={()=>{const base=workspacePortalUrl(true);if(base)window.open(`${base}/?workspace=1`,'_blank')}}><PanelsTopLeft size={18}/>Open Web Workspace</button></div><div className="publishCard publishCardPro"><div><span className="status"><CheckCircle2 size={14}/>{busy?'Publishing…':'Ready to publish'}</span><h2>{project?.report.name}</h2><ul><li>{project?.report.pages.length} report pages</li><li>{Object.keys(project?.model.measures||{}).length} semantic measures</li><li>{project?.security.roles.length} RLS role</li><li>Viewer separated from authoring</li></ul></div><button className="primary lg" onClick={go} disabled={busy}><ExternalLink size={18}/>{busy?'Publishing…':'Publish to Workspace'}</button></div>{err&&<div className="publishError"><b>Publish did not complete</b><span>{err}</span><button onClick={go} disabled={busy}>{busy?'Retrying…':'Retry Publish'}</button></div>}{published&&<div className="publishedSuccess"><CheckCircle2 size={20}/><div><b>Published successfully</b><span>Open the web workspace to configure who can access this report and how it is shared.</span><code>{portalUrl!=='Web Workspace'?`${portalUrl}/?workspace=1&report=${published.id}&share=1`:'Set VITE_WEB_URL or enter it when prompted'}</code></div><button className="primary" onClick={()=>openWorkspaceReport(published.id,true)}><Eye size={16}/>Open Workspace & Share</button></div>}<div className="publishedOnPage"><div className="sectionTitle"><div><span className="eyebrow">LIVE WORKSPACE</span><h2>Published reports</h2></div></div>{!publishedList.length?<div className="workspaceEmpty compact"><span>{err?'Published reports could not be loaded.':'No published reports yet.'}</span></div>:<div className="publishList">{publishedList.map(x=><div key={x.id}><div><b>{x.name}</b><small>{x.pages||project?.report.pages.length||0} pages · {x.visuals||project?.report.pages.reduce((n:any,p:any)=>n+(p.visuals?.length||0),0)||0} visuals</small></div><button onClick={()=>openWorkspaceReport(x.id,false)}><Eye size={15}/>View</button></div>)}</div>}</div></div>
}
function Copilot(){const{project}=useStudio();const[answer,setAnswer]=useState('');const explain=()=>setAnswer(`I found ${Object.keys(project?.model.tables||{}).length} semantic tables and ${Object.keys(project?.model.measures||{}).length} measures. Sales is the fact table. Customer, Product, Date and Region form a star schema. Recommended executive KPIs: Revenue, Gross Profit, Gross Margin %, Orders, Average Order Value and Customers.`);return <div className="page"><div className="copilotHero"><span className="spark">✦</span><div><span className="eyebrow">VTAB COPILOT</span><h1>Build with your data,<br/>not around it.</h1></div></div><div className="promptCards"><button onClick={explain}>Explain this semantic model</button><button onClick={()=>setAnswer('Suggested page: Revenue, Gross Profit and Margin KPI cards; Revenue Trend; Revenue by Region; Revenue by Product Category; Top Customers; Year and Region slicers.')}>Build an executive sales page</button><button onClick={()=>setAnswer('Relationship check: the current model is a clean star schema with four one-to-many active relationships into Sales.')}>Find model issues</button><button onClick={()=>setAnswer('Suggested measure: Revenue per Customer = DIVIDE([Revenue], [Customers]). Validate it with the deterministic measure engine before saving.')}>Recommend a new KPI</button></div>{answer&&<div className="aiAnswer"><span>✦</span><p>{answer}</p></div>}</div>}


class AuthoringErrorBoundary extends Component<{children:any},{error:any}>{
  state={error:null};
  static getDerivedStateFromError(error:any){return {error}}
  componentDidCatch(error:any,info:any){console.error('VTAB authoring render error',error,info)}
  render(){
    if(this.state.error){
      return <div className="authoringCrash"><div className="authoringCrashCard"><b>Report Designer could not render</b><p>{String(this.state.error?.message||this.state.error)}</p><button onClick={()=>location.reload()}>Reload Report Designer</button><small>The application is still running. This diagnostic replaces the previous blank screen behavior.</small></div></div>
    }
    return this.props.children
  }
}

function WebWorkspaceShell(){const[tab,setTab]=useState<'reports'|'admin'>('reports');const[isAdmin,setIsAdmin]=useState(false);useEffect(()=>{api<any>('/auth/me').then(u=>setIsAdmin((u.memberships||[]).some((m:any)=>m.role==='Admin'))).catch(()=>setIsAdmin(false))},[]);return <div className="webPortalShell"><div className="webPortalBar"><div><b>VTAB Web</b><span>Workspace & Access Portal</span></div><nav><button className={tab==='reports'?'active':''} onClick={()=>setTab('reports')}>Published Reports</button>{isAdmin&&<button className={tab==='admin'?'active':''} onClick={()=>setTab('admin')}>Administration</button>}</nav></div><main>{tab==='admin'&&isAdmin?<AdminWorkbench/>:<WorkspaceView/>}</main></div>}

function StudioApp(){const viewerId=new URLSearchParams(location.search).get('viewer');const workspaceOnly=new URLSearchParams(location.search).get('workspace');const{project,view,load,loading,error}=useStudio();useEffect(()=>{document.documentElement.dataset.theme=project?.appTheme||'vtab';document.documentElement.dataset.density=project?.uiDensity||'comfortable';document.documentElement.style.setProperty('--brand-accent',(project?.appAccent||'#2563eb').toLowerCase()==='#22d3ee'?'#2563eb':(project?.appAccent||'#2563eb'))},[project?.appTheme,project?.uiDensity,project?.appAccent]);useEffect(()=>{load()},[]);if(viewerId)return <PublishedViewer reportId={viewerId}/>;if(workspaceOnly==='1'&&project)return <WebWorkspaceShell/>;if(!project){if(error)return <div className="loading loadingError"><div className="brandMark">!</div><h2>Reporting Studio could not connect to the backend</h2><p>{error}</p><div className="loadingActions"><button className="primary" onClick={()=>load()}>Retry</button><button onClick={()=>window.open('http://127.0.0.1:8820/api/v1/health','_blank')}>Open API Health</button></div><small>Run DIAGNOSE_410.bat if the API health page does not open.</small></div>;return <div className="loading"><div className="brandMark">V</div><span>{loading?'Connecting to semantic engine…':'Loading Reporting Studio…'}</span></div>;}let body:any=<Home/>;if(view==='data')body=<DataWorkbench/>;if(view==='transform')body=<TransformWorkbench/>;if(view==='model')body=<ModelView/>;if(view==='ai-measures')body=<AIMeasureCatalog/>;if(view==='measures')body=<MeasuresWorkbench/>;if(view==='reports')body=<SavedReports/>;if(view==='report')body=<ReportWorkbench/>;if(view==='security')body=<SecurityWorkbench/>;if(view==='publish')body=<PublishView/>;if(view==='packages')body=<PackageWorkbench/>;if(view==='copilot')body=<Copilot/>;return <div className="appShell"><Sidebar/><div className="content"><Topbar/><main><AuthoringErrorBoundary>{body}</AuthoringErrorBoundary></main></div></div>}


function AuthoringLoginGate({onSignedIn}:{onSignedIn:()=>void}){
 const[mode,setMode]=useState<"login"|"forgot"|"reset">("login");
 const[login,setLogin]=useState({email:'',password:'',otp:'',newPassword:''});
 const[busy,setBusy]=useState(false);const[err,setErr]=useState('');const[info,setInfo]=useState('');const[resetProvider,setResetProvider]=useState<'backend'|'supabase'>('backend');
 const SUPABASE_CONFIGURED=!!(import.meta.env.VITE_SUPABASE_URL&&import.meta.env.VITE_SUPABASE_ANON_KEY);
 const submit=async()=>{
   setBusy(true);setErr('');setInfo('');
   try{
     if(SUPABASE_CONFIGURED){
       const{supabase:sb}=await import('./supabase');
       if(!sb)throw new Error('Supabase client failed to initialize.');
       if(mode==='forgot'){
          const email=login.email.trim().toLowerCase();
          if(!email)throw new Error('Enter your registered email address.');
          try{
            await api('/auth/password-reset/request',{method:'POST',body:JSON.stringify({email})});
            setResetProvider('backend');
          }catch(apiError:any){
            const apiMessage=apiError.message||String(apiError);
            if(!apiMessage.includes('Cannot reach VTAB API'))throw apiError;
            const recoveryUrl=`${window.location.origin}${window.location.pathname}`;
            const{error}=await sb.auth.resetPasswordForEmail(email,{redirectTo:recoveryUrl});
            if(error)throw error;
            setResetProvider('supabase');
          }
          setInfo('A 6-digit reset code was sent to your email.');
          setMode('reset');
       }else if(mode==='reset'){
         const email=login.email.trim().toLowerCase();
         const token=login.otp.trim();
         if(!email||!token||!login.newPassword)throw new Error('Enter email, 6-digit OTP and new password.');
         if(token.length!==6)throw new Error('Enter the 6-digit OTP from your email.');
         if(login.newPassword.length<6)throw new Error('Password must be at least 6 characters.');
         if(resetProvider==='backend'){
           await api('/auth/password-reset/confirm',{method:'POST',body:JSON.stringify({email,otp:token,newPassword:login.newPassword})});
           const{data,error}=await sb.auth.signInWithPassword({email,password:login.newPassword});
           if(error)throw error;
           if(data.session)localStorage.setItem('vtab_supabase_token',data.session.access_token);
           setInfo('Password reset successfully.');
           onSignedIn();
         }else{
           const{data,error}=await sb.auth.verifyOtp({email,token,type:'recovery'});
           if(error)throw error;
           const{error:updateError}=await sb.auth.updateUser({password:login.newPassword});
           if(updateError)throw updateError;
           if(data.session)localStorage.setItem('vtab_supabase_token',data.session.access_token);
           setInfo('Password reset successfully.');
           onSignedIn();
         }
       }else{
         const{data,error}=await sb.auth.signInWithPassword({email:login.email,password:login.password});
         if(error)throw error;
         if(data.session)localStorage.setItem('vtab_supabase_token',data.session.access_token);
         onSignedIn();
       }
     } else {
       const r=await api<any>('/auth/login',{method:'POST',body:JSON.stringify(login)});
       localStorage.setItem('vtab_workspace_token',r.token);
       onSignedIn();
     }
   }catch(e:any){const message=e.message||String(e);const recoveryEmailFailed=message.includes('Error sending recovery email')||message.includes('HTTP 500')||message.includes('HTTP 504')||message.includes('unexpected_failure');setErr(recoveryEmailFailed?'Supabase could not send the OTP email. Fix Supabase SMTP/Brevo settings: verified sender email, correct Brevo SMTP login/key, port 587, and Reset Password template using {{ .Token }}.':message);}finally{setBusy(false);}
 };
 const title = mode==="forgot" ? "Reset your password" : mode==="reset" ? "Enter reset OTP" : "Sign in to VTAB Reporting Studio";
 const buttonText = busy ? (mode==="forgot"?"Sending OTP…":mode==="reset"?"Resetting password…":"Signing in…") : (mode==="forgot"?"Send 6-digit OTP":mode==="reset"?"Reset Password":"Sign In");
 const disabled = busy || !login.email || (mode==="login"&&!login.password) || (mode==="reset"&&(!login.otp||!login.newPassword));
 return <div className="authoringLogin"><div className="authoringLoginCard"><div className="brandMark">V</div><div><small>SECURE AUTHORING</small><h1>{title}</h1><p>{SUPABASE_CONFIGURED?(mode==="login"?"Use your VTAB workspace account. New accounts can only be created from the web portal.":"Use the 6-digit OTP sent to your registered email to reset your password."):'Use your local administrator account.'}</p></div><label>Email<input autoFocus={mode==="login"||mode==="forgot"} type="email" value={login.email} placeholder={SUPABASE_CONFIGURED?'your@email.com':'admin@vtab.local'} onChange={e=>setLogin({...login,email:e.target.value})} onKeyDown={e=>e.key==='Enter'&&submit()}/></label>{mode==="login"&&<label>Password<input type="password" value={login.password} onChange={e=>setLogin({...login,password:e.target.value})} onKeyDown={e=>e.key==='Enter'&&submit()}/></label>}{mode==="reset"&&<label>6-digit OTP<input inputMode="numeric" maxLength={6} value={login.otp} onChange={e=>setLogin({...login,otp:e.target.value.replace(/\D/g,'').slice(0,6)})} onKeyDown={e=>e.key==='Enter'&&submit()}/></label>}{mode==="reset"&&<label>New Password<input type="password" value={login.newPassword} onChange={e=>setLogin({...login,newPassword:e.target.value})} onKeyDown={e=>e.key==='Enter'&&submit()}/></label>}{err&&<div className="authoringLoginError">{err}</div>}{info&&<div style={{color:"#16a34a",fontSize:12,marginBottom:8}}>{info}</div>}<button className="primary" onClick={submit} disabled={disabled}>{buttonText}</button>{!SUPABASE_CONFIGURED&&<div className="bootstrapHint"><b>First-run administrator</b><span>admin@vtab.local</span><span>Change the bootstrap password immediately after first sign-in.</span></div>}{SUPABASE_CONFIGURED&&<div className="bootstrapHint">{mode==="login"?<><span>No account? Register at the web portal.</span><button style={{background:"none",border:"none",color:"#2563eb",cursor:"pointer",padding:0,fontWeight:700,marginLeft:12}} onClick={()=>{setErr("");setInfo("");setMode("forgot");}}>Forgot password?</button></>:<><span>Remember your password?</span><button style={{background:"none",border:"none",color:"#2563eb",cursor:"pointer",padding:0,fontWeight:700,marginLeft:12}} onClick={()=>{setErr("");setInfo("");setMode("login");}}>Sign in</button>{mode==="reset"&&<button style={{background:"none",border:"none",color:"#2563eb",cursor:"pointer",padding:0,fontWeight:700,marginLeft:12}} onClick={()=>{setErr("");setInfo("");setMode("forgot");}}>Resend OTP</button>}</>}</div>}</div></div>
}

// WORKSPACE_ONLY mode — set VITE_APP_MODE=WORKSPACE_ONLY at build time for cloud web deployment.
// When set, the entire authoring studio is excluded and only the cloud viewer/portal is shown.
const WORKSPACE_ONLY=import.meta.env.VITE_APP_MODE==='WORKSPACE_ONLY';

function CloudPortal(){
 const[cloudSession,setCloudSession]=useState<any>(null);
 const[cloudReady,setCloudReady]=useState(false);
 useEffect(()=>{document.documentElement.dataset.theme='vtab';document.documentElement.dataset.density='comfortable'},[]);
 useEffect(()=>{
   import('./supabase').then(({supabase})=>{
     if(!supabase){setCloudReady(true);return;}
     supabase.auth.getSession().then(({data})=>{
       if(data.session){localStorage.setItem('vtab_supabase_token',data.session.access_token);setCloudSession(data.session);}
       setCloudReady(true);
     });
     const{data:{subscription}}=supabase.auth.onAuthStateChange((_,session)=>{if(session)localStorage.setItem('vtab_supabase_token',session.access_token);else localStorage.removeItem('vtab_supabase_token');setCloudSession(session)});
     return()=>subscription.unsubscribe();
   });
 },[]);
 if(!cloudReady)return <div className="loading"><div className="brandMark">V</div><span>Loading workspace…</span></div>;
 if(!cloudSession)return <SupabaseAuthGate onSignedIn={setCloudSession}/>;
 return <CloudWorkspace session={cloudSession}/>;
}

export default function Studio(){
 const params=new URLSearchParams(location.search);const consumer=!!params.get('viewer')||params.get('workspace')==='1'||!!params.get('report');
 if(WORKSPACE_ONLY)return <CloudPortal/>;
 if(consumer&&isSupabaseConfigured)return <CloudPortal/>;

 // ── Existing desktop / local path — completely unchanged ───────────────────
 const[ready,setReady]=useState(consumer),[signedIn,setSignedIn]=useState(consumer);
 useEffect(()=>{if(consumer)return;api<any>('/auth/status').then(async s=>{if(!s.authoringRequired){setSignedIn(true);setReady(true);return}try{await api('/auth/me');setSignedIn(true)}catch{}finally{setReady(true)}}).catch(()=>setReady(true))},[consumer]);
 if(consumer)return <StudioApp/>;
 if(!ready)return <div className="loading"><div className="brandMark">V</div><span>Checking secure session…</span></div>;
 if(!signedIn)return <AuthoringLoginGate onSignedIn={()=>setSignedIn(true)}/>;
 return <StudioApp/>;
}
